# build.yml
# Reusable workflow that builds, packs, and publishes AudibleApi.
---
name: build

on:
  workflow_call:
    inputs:
      version_override:
        type: string
        description: 'Version number override'
        required: false
      project_file:
        type: string
        description: 'Path to the .csproj file'
        required: true
      nuspec_file:
        type: string
        description: 'Path to the .nuspec file'
        required: true

env:
  DOTNET_CONFIGURATION: 'Release'
  DOTNET_VERSION: '10.x'
  NUGET_SOURCE: 'https://api.nuget.org/v3/index.json'

jobs:
  build:
    name: Build and Publish
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v5
      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Get version
        id: get_version
        working-directory: 
        run: |
          inputVersion="${{ inputs.version_override }}"
          if [[ "${#inputVersion}" -gt 0 ]]
          then
            version="${inputVersion}"
          else
            version="$(grep -Eio -m 1 '<Version>.*</Version>' ${{ inputs.project_file }} | sed -r 's/<\/?Version>//g')"
          fi
          echo "version=${version}" >> "${GITHUB_OUTPUT}"

      - name: Unit Tests
        run: |
          dotnet test

      - name: Build and Pack
        id: build_pack
        run: |
          nuspec_relative=$(realpath --relative-to="$(dirname ${{ inputs.project_file }})" "${{ inputs.nuspec_file }}")
          dotnet build ${{ inputs.project_file }} -c ${{ env.DOTNET_CONFIGURATION }}
          dotnet pack ${{ inputs.project_file }} --no-build -c ${{ env.DOTNET_CONFIGURATION }} -o . -p:NuspecFile="$nuspec_relative" -p:NuspecProperties="version=${{ steps.get_version.outputs.version }}"
          nupkg=$(ls *.nupkg)
          echo "nupkg=${nupkg}" >> "${GITHUB_OUTPUT}"

      - name: Upload Artifact
        uses: actions/upload-artifact@v5
        with:
          name: '${{ steps.build_pack.outputs.nupkg }}'
          path: ./${{ steps.build_pack.outputs.nupkg }}
          if-no-files-found: error
          retention-days: 7

      - name: NuGet login
        if: github.event_name == 'push'
        uses: NuGet/login@v1
        id: login
        with:
          user: rmcrackan

      - name: Publish nupkg
        if: github.event_name == 'push'
        run: |
          dotnet nuget push '${{ steps.build_pack.outputs.nupkg }}' -s ${{ env.NUGET_SOURCE }} -k ${{ steps.login.outputs.NUGET_API_KEY }} --skip-duplicate
